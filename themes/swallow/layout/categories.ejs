<% if (page.photos && page.photos != ''){ %>
  <div class="cover">
<% } else { %>
  <div class="cover">
<% } %>
  <h2 class="title"><%= __('category') %></h2>
  <p class="desc"><%= __('categories') %>：<%= site.categories.length %></p>
</div>
<div class="inner">
  <div class="archive category">
    <%- list_categories(site.categories) %>
  </div>
</div>
